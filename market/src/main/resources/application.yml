spring:
    application.name: market
    datasource:
        url: jdbc:mysql://localhost:3308/dbmarket?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
        username: appuser
        password: ${DBUSERS_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        show-sql: true

